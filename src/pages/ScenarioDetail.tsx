import { catalogEntries } from '../domain/catalog';
import { StatusBadge } from '../components/StatusBadge';

export function ScenarioDetail({ id, navigate }: { id?: string; navigate: (route: string) => void }) {
  const entry = catalogEntries.find((item) => item.id === id) ?? catalogEntries[0];
  return <div className="page-stack"><button className="back-link" type="button" onClick={() => navigate('/catalog')}>← 返回场景目录</button><section className="detail-hero"><div><span className="eyebrow">{entry.domain} / {entry.projectSource}</span><h1>{entry.userVisibleName}</h1><p>{entry.purpose}</p></div><StatusBadge status="处理中">业务场景</StatusBadge></section><div className="detail-grid"><section className="panel detail-panel"><span className="eyebrow">01 / 业务定义</span><h2>谁在什么情况下使用</h2><div className="detail-lines"><p><b>适用角色</b>{entry.applicableUsers.join('、')}</p><p><b>进入条件</b>{entry.prerequisites.join('；')}</p><p><b>处理输入</b>{entry.inputs.join('；')}</p><p><b>关联对象</b>{entry.relatedObjects.join('、')}</p></div></section><section className="panel detail-panel"><span className="eyebrow">02 / 处理路径</span><h2>从输入走到业务结果</h2><ol className="detail-steps">{entry.steps.map((step, index) => <li key={step}><span>0{index + 1}</span>{step}</li>)}</ol></section><section className="panel detail-panel"><span className="eyebrow">03 / 结果沉淀</span><h2>确认后，数据回到哪里</h2><div className="detail-lines"><p><b>确认点</b>{entry.confirmationPoints.join('；')}</p><p><b>业务结果</b>{entry.result}</p><p><b>写回位置</b>{entry.writeback}</p><p><b>下一步</b>{entry.nextAction}</p></div></section><section className="panel detail-panel"><span className="eyebrow">04 / 持续运营</span><h2>任务、通知与异常恢复</h2><div className="detail-lines"><p><b>任务</b>{entry.taskEffects.join('；')}</p><p><b>通知</b>{entry.notificationEffects.join('；')}</p><p><b>异常</b>{entry.exceptionRecovery.join('；')}</p><p><b>入口</b>{entry.webEntry} · {entry.miniProgramEntry}</p></div></section></div></div>;
}
